{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace workspace",
  "outputDirectory": "workspace/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
