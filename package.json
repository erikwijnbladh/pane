{
  "name": "pane",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"node server.js\" \"npm run dev --prefix app\" \"npm run dev --prefix workspace\"",
    "install:all": "npm install && npm install --prefix app && npm install --prefix workspace"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
